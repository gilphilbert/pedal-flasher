{
  "name": "typescript",
  "version": "0.0.1",
  "description": "Web Flasher for DIY FFB Pedal",
  "source": "src/index.html",
  "scripts": {
    "dev": "npm run clean && parcel src/index.html",
    "build": "npm run clean && parcel build src/index.html --no-optimize --public-url ./ --dist-dir docs",
    "clean": "rimraf docs .parcel-cache",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.8.3",
    "parcel-resolver-ignore": "^2.1.5",
    "rimraf": "^4.1.2",
    "typescript": "^4.9.4",
    "web-serial-polyfill": "^1.0.15"
  },
  "dependencies": {
    "esptool-js": "^0.4.7"
  }
}
